{% set name = "twisted-iocpsupport" %}
{% set version = "25.2.0" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.org/packages/source/{{ name[0] }}/{{ name }}/{{ name | replace('-', '_') }}-{{ version }}.tar.gz
  sha256: 6fc1650eac1f342ebc974fc969c6de1658dc2e051c2bd11fa4a4a6fe4fe11f89

build:
  number: 1
  skip: true  # [not win]
  skip: true  # [py<35]
  script: {{ PYTHON }} -m pip install . -vv

requirements:
  build:
    - {{ compiler('c') }}
    - {{ stdlib("c") }}
  host:
    - cython
    - pip
    - python
    - setuptools
  run:
    - python

test:
  imports:
    - twisted_iocpsupport
  commands:
    - pip check
  requires:
    - pip

about:
  home: https://github.com/twisted/twisted-iocpsupport
  summary: An extension for use in the twisted I/O Completion Ports reactor.
  license: MIT
  license_file: LICENSE

extra:
  recipe-maintainers:
    - nehaljwani
